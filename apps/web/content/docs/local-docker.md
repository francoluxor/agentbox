---
title: Local Docker
description: The default provider — one local container per box, isolated by a per-box git branch.
---

> **Draft** — this page is a stub; prose is coming soon.

Use Local Docker for fast, free, fully local boxes on your own machine.

## Requirements

_Docker Desktop or OrbStack, and Node `>=20.10`._

## First-run image

_The one-time `agentbox/box:dev` image pull/build._

## Resource limits

_`--memory`, `--cpus`, `--pids-limit`, `--disk` caps._

## Notes

_Pause/resume behavior and shared docker cache._
