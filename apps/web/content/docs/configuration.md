---
title: Configuration keys
description: User config keys and how the layers resolve.
---

> **Draft** — this page is a stub; prose is coming soon.

## Config layers

_Precedence: CLI > workspace > project > global > committed defaults > built-in._

## box.*

_Image, provider, sizes, checkpoints, resource limits, isolation flags._

## agent / code / shell

_Session names, IDE preference, default shell behavior._

## Providers & sizes

_Per-provider image and size overrides._

## queue & autopause

_Background queue and idle auto-pause settings._

## The config command

_`config get` / `set` / `unset` / `list` / `path` / `edit`._
