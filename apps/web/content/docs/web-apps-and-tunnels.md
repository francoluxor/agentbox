---
title: Web apps & tunnels
description: Expose a box web app at the same URL inside the box and on your host.
---

> **Draft** — this page is a stub; prose is coming soon.

## Expose a web service

_Mark a service with `expose` in `agentbox.yaml` to forward its port._

## Readiness

_`ready_when` probes (port / log / http) before the URL goes live._

## Same URL inside & out

_Portless gives the box and the host the same `https://<box>.localhost` URL._

## Cloud preview URLs

_Public HTTPS preview URLs on the cloud providers._

## Extra ports

_Forwarding additional ports beyond the exposed web service._
