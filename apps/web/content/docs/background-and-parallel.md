---
title: Background & parallel boxes
description: Run many boxes at once and queue background agent runs with -i.
---

> **Draft** — this page is a stub; prose is coming soon.

## Run boxes in parallel

_Multiple independent boxes, each on its own branch._

## Queue background runs

_The `-i` flag to launch an agent run in the background._

## Manage the queue

_`agentbox queue list` / `show` / `cancel`._

## Monitor

_`agentbox top`, `ls`, and the dashboard for a live overview._
