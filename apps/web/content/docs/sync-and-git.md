---
title: Sync & git push
description: Get work back to the host and push through the relay with explicit approval.
---

> **Draft** — this page is a stub; prose is coming soon.

## Download your workspace

_`agentbox download` — gitignore-aware copy of `/workspace` back to the host._

## Copy files

_`agentbox cp` for one-off file copies in either direction._

## Push & pull via the relay

_`agentbox git push` / `pull` — credentials never enter the box._

## Pull requests

_Creating and checking out GitHub PRs through the relay._

## Git permissions

_How each remote write triggers a host-side approval prompt._
