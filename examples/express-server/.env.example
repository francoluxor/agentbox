# Copy this file to `.env` and fill in real values. The agentbox first-run
# wizard should pick up on the GREETING requirement (server.js exits without
# it) and either declare it as a service env var in the generated
# agentbox.yaml, or remind the user to copy this file before starting.

PORT=3000
GREETING=hello from agentbox
